sitemaps:
  default:
    lastmod: YYYY-MM-DD
    languages:
       it:
         source: /query-index.json
         destination: /sitemap.xml
         hreflang: it
       en:
         source: /en/query-index.json
         destination: /en/sitemap.xml
         hreflang: en
