sitemaps:
  default:
    lastmod: YYYY-MM-DD
    languages:
       it:
         source: /query-index.json
         destination: /sitemap.xml
         origin: https://main--unipol-eds--comwrap-it.aem.live/
         hreflang: it
